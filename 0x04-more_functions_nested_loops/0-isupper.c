#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: return 1 for uppercase and 0 for any other
 */

int _isupper(int c)
{
	int c = 'A'-'Z'

	if (c >= 'A' && c <= 'Z')
	{
		_putchar("A" c++)

		return (1);
	}
	else
	{
		return (0);
	}
}
